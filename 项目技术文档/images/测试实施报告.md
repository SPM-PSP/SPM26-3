
## 3. 接口测试

### 3.1 用户（User）模块接口测试

| 序号 | 需求规格 | 操作步骤 | 期望输出 | 实际输出 | 通过与否 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | **正常**-微信登录 | `POST /api/v1/auth/login/wechat`<br>请求体:<br>```json<br>{<br>  "code": "test_code",<br>  "nickname": "Test User",<br>  "avatarUrl": "http://example.com/avatar.jpg"<br>}<br>``` | `code: 200`<br>响应体 `data` 包含 `token` 和 `userInfo` (id, nickname, avatarUrl, verifyStatus)。 | 与期望一致：返回 200 且包含 token 和 userInfo | 通过 | |
| 2 | **正常**-获取个人信息 | `GET /api/v1/user/profile`<br>（需在 Header 中携带 Token） | `code: 200`<br>响应体 `data` 包含 `id`, `nickname`, `avatarUrl`, `school`, `grade`, `gender` 等完整信息。 | 与期望一致：返回 200 且信息完整 | 通过 | |
| 3 | **异常**-未登录获取个人信息 | `GET /api/v1/user/profile`<br>（不带 Token） | `code: 401`<br>消息提示未授权或需要登录。 | 与期望一致：返回 401 且提示未授权 | 通过 | API规范性检查 |
| 4 | **正常**-查看他人主页 | `GET /api/v1/user/{userId}/profile` | `code: 200`<br>响应体 `data` 包含 `id`, `nickname`, `avatarUrl`, `school`, `grade`, `isFollow` 等公开信息。 | 与期望一致：返回 200 且公开信息完整 | 通过 | |
| 5 | **异常**-查看不存在的用户主页 | `GET /api/v1/user/99999/profile` | `code: 404`<br>消息提示用户不存在。 | 与期望一致：返回 404 且提示不存在 | 通过 | API规范性检查 |
| 6 | **正常**-修改个人信息 | `PUT /api/v1/user/profile`<br>（需在 Header 中携带 Token）<br>请求体:<br>```json<br>{<br>  "nickname": "Updated Nickname"<br>}<br>``` | `code: 200`<br>响应体 `data` 中 `nickname` 已更新。 | 与期望一致：返回 200 且昵称更新 | 通过 | |
| 7 | **正常**-关注用户 | `POST /api/v1/user/{userId}/follow`<br>（需在 Header 中携带 Token） | `code: 200`<br>消息为 "关注成功"。 | 与期望一致：返回 200 且提示关注成功 | 通过 | |
| 8 | **异常**-关注自己 | `POST /api/v1/user/{myUserId}/follow`<br>（用自己的ID关注自己） | `code: 400` 或 `500`<br>消息提示不能关注自己。 | 与期望一致：返回错误码且提示不能关注自己 | 通过 | API规范性检查 |
| 9 | **异常**-重复关注 | `POST /api/v1/user/{userId}/follow`<br>（对已关注用户再次操作） | `code: 400` 或 `500`<br>消息提示已关注该用户。 | 与期望一致：返回错误码且提示已关注 | 通过 | |
| 10 | **正常**-取消关注用户 | `DELETE /api/v1/user/{userId}/follow`<br>（需在 Header 中携带 Token） | `code: 200`<br>消息为 "已取消关注"。 | 与期望一致：返回 200 且提示取消关注 | 通过 | |
| 11 | **正常**-申请学生认证 | `POST /api/v1/verification/student/apply`<br>请求体:<br>```json<br>{<br>  "realName": "张三",<br>  "studentNo": "20210001",<br>  "school": "XX大学",<br>  "proofImageUrl": "https://example.com/proof.jpg"<br>}<br>``` | `code: 200`<br>消息为 "认证申请已提交，请等待审核"。 | 与期望一致：返回 200 且提示已提交 | 通过 | |
| 12 | **异常**-申请认证信息不全 | `POST /api/v1/verification/student/apply`<br>请求体:<br>```json<br>{<br>  "realName": "张三"<br>}<br>``` | `code: 400`<br>消息提示缺少必要字段。 | 与期望一致：返回 400 且提示缺少字段 | 通过 | API规范性检查 |
| 13 | **正常**-查看认证状态 | `GET /api/v1/verification/student/status` | `code: 200`<br>响应体 `data` 包含 `status` 和 `application` (id, realName, status等) 信息。 | 与期望一致：返回 200 且包含状态信息 | 通过 | |
| 14 | **正常**-搜索用户 | `GET /api/v1/users/search?keyword=test` | `code: 200`<br>响应体 `data` 为一个用户数组，每个用户包含 `id`, `nickname`, `avatarUrl`。 | 与期望一致：返回 200 且数组字段完整 | 通过 | |
| 15 | **正常**-获取粉丝列表 | `GET /api/v1/user/{userId}/followers` | `code: 200`<br>响应体 `data` 为一个用户数组，每个用户包含 `id`, `nickname`, `avatarUrl`。 | 与期望一致：返回 200 且数组字段完整 | 通过 | |
| 16 | **正常**-获取关注列表 | `GET /api/v1/user/{userId}/following` | `code: 200`<br>响应体 `data` 为一个用户数组，每个用户包含 `id`, `nickname`, `avatarUrl`。 | 与期望一致：返回 200 且数组字段完整 | 通过 | |

### 3.2 结伴帖子（Post）模块接口测试

| 序号 | 需求规格 | 操作步骤 | 期望输出 | 实际输出 | 通过与否 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | **正常**-发布结伴帖子 | `POST /api/v1/post`<br>请求体:<br>```json<br>{<br>  "title": "Test Post",<br>  "content": "This is a test post."<br>}<br>``` | `code: 200`<br>响应体 `data` 包含 `id`, `authorId`, `title`, `content`, `status` 等新帖子信息。 | 与期望一致：返回 200 且 data 含新帖子信息 | 通过 | |
| 2 | **异常**-未登录发布帖子 | `POST /api/v1/post`<br>（不带 Token） | `code: 401`<br>消息提示未授权。 | 与期望一致：返回 401 且提示未授权 | 通过 | API规范性检查 |
| 3 | **正常**-获取帖子列表 | `GET /api/v1/post` | `code: 200`<br>响应体 `data` 为帖子数组，每个帖子包含 `id`, `authorId`, `title`, `coverImage` 等信息。 | 与期望一致：返回 200 且数组字段完整 | 通过 | |
| 4 | **正常**-获取帖子详情 | `GET /api/v1/post/{postId}` | `code: 200`<br>响应体 `data` 包含 `id`, `authorId`, `title`, `content`, `imageUrls` 等完整信息。 | 与期望一致：返回 200 且详情完整 | 通过 | |
| 5 | **异常**-获取不存在的帖子 | `GET /api/v1/post/99999` | `code: 404`<br>消息提示帖子不存在。 | 与期望一致：返回 404 且提示不存在 | 通过 | API规范性检查 |
| 6 | **正常**-修改自己的帖子 | `PUT /api/v1/post/{myPostId}`<br>请求体:<br>```json<br>{<br>  "title": "Updated Post Title"<br>}<br>``` | `code: 200`<br>响应体 `data` 中 `title` 已更新。 | 与期望一致：返回 200 且标题更新 | 通过 | |
| 7 | **异常**-修改他人的帖子 | `PUT /api/v1/post/{otherUserPostId}`<br>（使用用户A的Token修改用户B的帖子） | `code: 403`<br>消息提示无权限操作。 | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |
| 8 | **正常**-删除自己的帖子 | `DELETE /api/v1/post/{myPostId}` | `code: 200`<br>消息为 "删除成功"。 | 与期望一致：返回 200 且提示删除成功 | 通过 | |
| 9 | **异常**-删除他人的帖子 | `DELETE /api/v1/post/{otherUserPostId}`<br>（使用用户A的Token删除用户B的帖子） | `code: 403`<br>消息提示无权限操作。 | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |
| 10 | **正常**-申请加入结伴 | `POST /api/v1/post/{postId}/apply`<br>请求体:<br>```json<br>{<br>  "message": "I want to join!"<br>}<br>``` | `code: 200`<br>消息为 "申请已提交"。 | 与期望一致：返回 200 且提示已提交 | 通过 | |
| 11 | **异常**-重复申请 | `POST /api/v1/post/{postId}/apply`<br>（对已申请的帖子再次申请） | `code: 400` 或 `500`<br>消息提示已申请，请勿重复提交。 | 与期望一致：返回错误码且提示重复提交 | 通过 | |
| 12 | **正常**-群主查看申请列表 | `GET /api/v1/post/{myPostId}/applications` | `code: 200`<br>响应体 `data` 为申请数组，每个申请包含 `id`, `applicantId`, `message`, `status`。 | 与期望一致：返回 200 且申请列表字段完整 | 通过 | |
| 13 | **异常**-非群主查看申请列表 | `GET /api/v1/post/{otherUserPostId}/applications` | `code: 403`<br>消息提示无权限操作。 | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |
| 14 | **正常**-群主审核申请 | `POST /api/v1/post/{myPostId}/applications/{appId}/review`<br>请求体:<br>```json<br>{<br>  "status": 1<br>}<br>``` | `code: 200`<br>消息为 "审核成功"。 | 与期望一致：返回 200 且提示审核成功 | 通过 | |
| 15 | **异常**-非群主审核申请 | `POST /api/v1/post/{otherUserPostId}/applications/{appId}/review` | `code: 403`<br>消息提示无权限操作。 | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |

### 3.3 动态（Moment）模块接口测试

| 序号 | 需求规格 | 操作步骤 | 期望输出 | 实际输出 | 通过与否 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | **正常**-发布动态 | `POST /api/v1/moment`<br>请求体:<br>```json<br>{<br>  "content": "My first moment!"<br>}<br>``` | `code: 200`<br>响应体 `data` 包含 `id`, `authorId`, `content`, `likeCount`, `media` 等新动态信息。 | 与期望一致：返回 200 且 data 含新动态信息 | 通过 | |
| 2 | **正常**-获取动态列表 | `GET /api/v1/moment` | `code: 200`<br>响应体 `data` 为动态数组，每个动态包含 `id`, `authorId`, `content`, `likeCount` 等信息。 | 与期望一致：返回 200 且列表字段完整 | 通过 | |
| 3 | **正常**-获取动态详情 | `GET /api/v1/moment/{momentId}` | `code: 200`<br>响应体 `data` 包含 `id`, `authorId`, `content`, `isLiked`, `isFavorited` 等完整信息。 | 与期望一致：返回 200 且详情字段完整 | 通过 | |
| 4 | **正常**-删除自己的动态 | `DELETE /api/v1/moment/{myMomentId}` | `code: 200`<br>消息为 "删除成功"。 | 与期望一致：返回 200 且提示删除成功 | 通过 | |
| 5 | **异常**-删除他人的动态 | `DELETE /api/v1/moment/{otherUserMomentId}` | `code: 403`<br>消息为 "无权限删除"。 | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |
| 6 | **正常**-点赞动态 | `POST /api/v1/moment/{momentId}/like` | `code: 200`<br>消息为 "点赞成功"。 | 与期望一致：返回 200 且提示点赞成功 | 通过 | |
| 7 | **异常**-对不存在的动态点赞 | `POST /api/v1/moment/99999/like` | `code: 404`<br>消息为 "动态不存在"。 | 与期望一致：返回 404 且提示不存在 | 通过 | API规范性检查 |
| 8 | **正常**-取消点赞 | `DELETE /api/v1/moment/{momentId}/like` | `code: 200`<br>消息为 "已取消点赞"。 | 与期望一致：返回 200 且提示已取消点赞 | 通过 | |
| 9 | **正常**-收藏动态 | `POST /api/v1/moment/{momentId}/favorite` | `code: 200`<br>消息为 "收藏成功"。 | 与期望一致：返回 200 且提示收藏成功 | 通过 | |
| 10 | **正常**-取消收藏 | `DELETE /api/v1/moment/{momentId}/favorite` | `code: 200`<br>消息为 "已取消收藏"。 | 与期望一致：返回 200 且提示取消收藏 | 通过 | |
| 11 | **正常**-发表评论 | `POST /api/v1/moment/{momentId}/comment`<br>请求体:<br>```json<br>{<br>  "content": "Nice!"<br>}<br>``` | `code: 200`<br>消息为 "评论成功"。 | 与期望一致：返回 200 且提示评论成功 | 通过 | |
| 12 | **异常**-评论内容为空 | `POST /api/v1/moment/{momentId}/comment`<br>请求体:<br>```json<br>{<br>  "content": ""<br>}<br>``` | `code: 400`<br>消息为 "评论内容不能为空"。 | 与期望一致：返回 400 且提示不能为空 | 通过 | API规范性检查 |
| 13 | **正常**-获取评论列表 | `GET /api/v1/moment/{momentId}/comment` | `code: 200`<br>响应体 `data` 为评论数组，每个评论包含 `id`, `userId`, `content`, `parentId`。 | 与期望一致：返回 200 且评论字段完整 | 通过 | |
| 14 | **正常**-删除自己的评论 | `DELETE /api/v1/moment/comment/{myCommentId}` | `code: 200`<br>消息为 "删除成功"。 | 与期望一致：返回 200 且提示删除成功 | 通过 | |
| 15 | **异常**-删除他人的评论 | `DELETE /api/v1/moment/comment/{otherUserCommentId}` | `code: 403`<br>消息为 "无权限删除"。 | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |
| 16 | **正常**-获取我点赞的动态 | `GET /api/v1/moments/me/likes` | `code: 200`<br>响应体 `data` 为动态数组，每个动态包含 `id`, `title`, `coverUrl`, `author`。 | 与期望一致：返回 200 且列表字段完整 | 通过 | |
| 17 | **正常**-获取我收藏的动态 | `GET /api/v1/moments/me/favorites` | `code: 200`<br>响应体 `data` 为动态数组，每个动态包含 `id`, `title`, `coverUrl`, `author`。 | 与期望一致：返回 200 且列表字段完整 | 通过 | |
| 18 | **正常**-获取我发布的评论 | `GET /api/v1/moments/me/comments` | `code: 200`<br>响应体 `data` 为评论数组，每个评论包含 `id`, `content`, `createdAt`, `moment`。 | 与期望一致：返回 200 且列表字段完整 | 通过 | |

### 3.4 群聊（Group）模块接口测试

| 序号 | 需求规格 | 操作步骤 | 期望输出 | 实际输出 | 通过与否 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | **正常**-发送群消息 | `POST /api/v1/groups/1/messages`<br>请求体:<br>```json<br>{"msgType":0,"content":"大家好！","mediaUrl":null}<br>``` | code:200，返回消息`id`、`groupId`、`senderId`、`msgType`、`content`、`mediaUrl`、`createdAt`的列表 | 与期望一致：返回 200 且消息字段完整 | 通过 | |
| 2 | **异常**-发送群消息（非成员） | `POST /api/v1/groups/1/messages`<br>（以非群成员身份）<br>请求体:<br>```json<br>{"msgType":0,"content":"大家好！","mediaUrl":null}<br>``` | code:403，message:“你不是该群成员，无法发送消息” | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |
| 3 | **异常**-发送群消息（已退出成员） | `POST /api/v1/groups/1/messages`<br>（以已退出群的用户身份）<br>请求体:<br>```json<br>{"msgType":0,"content":"大家好！","mediaUrl":null}<br>``` | code:403，message:“你不是该群成员，无法发送消息” | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |
| 4 | **正常**-获取我的群列表 | `GET /api/v1/users/me/groups`<br>（以群成员身份） | code:200，返回包含`GroupId`、`name`、`postId`、`ownerId`、`memberCount`、`createdAt`的列表 | 与期望一致：返回 200 且列表字段完整 | 通过 | |
| 5 | **正常**-获取我的群列表（不在任何群） | `GET /api/v1/users/me/groups`<br>（以不在任何群的用户身份） | code:200，data返回空列表[] | 与期望一致：返回 200 且 data 为空数组 | 通过 | |
| 6 | **正常**-退群（普通成员） | `DELETE /api/v1/groups/1/members/me`<br>（以普通成员身份） | code:200，message:“操作成功” | 与期望一致：返回 200 且提示操作成功 | 通过 | 需验证数据库 |
| 7 | **正常**-解散群（群主） | `DELETE /api/v1/groups/1/members/me`<br>（以群主身份） | code:200，message:“操作成功” | 与期望一致：返回 200 且提示操作成功 | 通过 | 需验证数据库 |
| 8 | **异常**-退群（已解散或不存在的群） | `DELETE /api/v1/groups/999/members/me` | code:404，message:“操作失败: 群聊不存在或已解散” | 与期望一致：返回 404 且提示不存在 | 通过 | API规范性检查 |
| 9 | **正常**-拉取历史消息 | `GET /api/v1/groups/1/messages`<br>（以群成员身份） | code:200，返回消息列表，按时间倒序排列 | 与期望一致：返回 200 且消息倒序 | 通过 | |
| 10 | **异常**-拉取历史消息（已退出群成员） | `GET /api/v1/groups/1/messages`<br>（以已退出群的用户身份） | code:403，message:“查询失败: 你不是该群成员，无法查看消息” | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |
| 11 | **异常**-拉取历史消息（不存在或已解散的群） | `GET /api/v1/groups/999/messages` | code:404，message:“查询失败: 群聊不存在或已解散” | 与期望一致：返回 404 且提示不存在 | 通过 | API规范性检查 |
| 12 | **正常**-修改群名（群主） | `PUT /api/v1/groups/1/name`<br>（以群主身份）<br>请求体:<br>```json<br>{"groupName":"新的群名"}<br>``` | code:200，message:“Group name updated” | 与期望一致：返回 200 且提示更新成功 | 通过 | 需验证数据库 |
| 13 | **异常**-修改群名（普通成员） | `PUT /api/v1/groups/1/name`<br>（以普通成员身份）<br>请求体:<br>```json<br>{"groupName":"普通成员改的群名"}<br>``` | code:403，message:“Failed to update group name: 只有群主可以修改群名” | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |
| 14 | **正常**-查询群成员列表 | `GET /api/v1/groups/1/members` | code:200，返回包含 `userId`、`nickname`、`avatarUrl`、`role`、`joinSource`、`joinedAt`的列表 | 与期望一致：返回 200 且成员字段完整 | 通过 | |
| 15 | **异常**-查询群成员列表（不存在的群） | `GET /api/v1/groups/999/members` | code:404，message:“Failed to get members: 群聊不存在或已解散” | 与期望一致：返回 404 且提示不存在 | 通过 | API规范性检查 |
| 16 | **正常**-查询群成员数 | `GET /api/v1/groups/1/member-count` | code:200，返回`groupId`和`memberCount` | 与期望一致：返回 200 且字段完整 | 通过 | |
| 17 | **异常**-查询群成员数（不存在的群） | `GET /api/v1/groups/999/member-count` | code:404，message:“Failed to get member count: 群聊不存在或已解散” | 与期望一致：返回 404 且提示不存在 | 通过 | API规范性检查 |
| 18 | **正常**-邀请成员加群（群主或管理员） | `POST /api/v1/groups/1/members`<br>（以群主或管理员身份）<br>请求体:<br>```json<br>{"invitedUserId":3}<br>``` | code:200，message:“邀请成功” | 与期望一致：返回 200 且提示邀请成功 | 通过 | 需验证数据库 |
| 19 | **异常**-邀请成员加群（普通成员） | `POST /api/v1/groups/1/members`<br>（以普通成员身份）<br>请求体:<br>```json<br>{"invitedUserId":5}<br>``` | code:403，message:“邀请失败: 只有群主或管理员可以邀请成员” | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |
| 20 | **异常**-邀请成员加群（不存在的群） | `POST /api/v1/groups/999/members`<br>请求体:<br>```json<br>{"invitedUserId":5}<br>``` | code:404，message:“邀请失败: 群聊不存在或已解散” | 与期望一致：返回 404 且提示不存在 | 通过 | API规范性检查 |
| 21 | **异常**-邀请已是群成员的用户 | `POST /api/v1/groups/1/members`<br>（邀请已是成员的用户）<br>请求体:<br>```json<br>{"invitedUserId":2}<br>``` | code:400，message:“邀请失败: 该用户已是群成员” | 与期望一致：返回 400 且提示已是成员 | 通过 | API规范性检查 |
| 22 | **正常**-设置管理员（群主） | `PUT /api/v1/groups/1/members/2/role`<br>（以群主身份） | code:200，message:“设置管理员成功” | 与期望一致：返回 200 且提示成功 | 通过 | 需验证数据库 |
| 23 | **异常**-设置管理员（非群主） | `PUT /api/v1/groups/1/members/2/role`<br>（以普通成员或管理员身份） | code:403，message:“设置失败: 只有群主可以设置管理员” | 与期望一致：返回 403 且提示无权限 | 通过 | API规范性检查 |
| 24 | **异常**-设置管理员（目标已是管理员） | `PUT /api/v1/groups/1/members/2/role`<br>（以群主身份对已是管理员的成员） | code:400，message:“设置失败: 该用户已经是管理员” | 与期望一致：返回 400 且提示已是管理员 | 通过 | API规范性检查 |
| 25 | **异常**-设置管理员（目标是群主） | `PUT /api/v1/groups/1/members/1/role`<br>（以群主身份对群主自身） | code:400，message:“设置失败: 不能修改群主的角色” | 与期望一致：返回 400 且提示不能修改群主 | 通过 | API规范性检查 |
| 26 | **异常**-设置管理员（不存在的群） | `PUT /api/v1/groups/999/members/2/role` | code:404，message:“设置失败: 群聊不存在或已解散” | 与期望一致：返回 404 且提示不存在 | 通过 | API规范性检查 |
| 27 | **异常**-设置管理员（已退出群成员） | `PUT /api/v1/groups/1/members/3/role`<br>（对已退出成员操作） | code:400，message:“设置失败: 该用户不是群成员” | 与期望一致：返回 400 且提示非成员 | 通过 | API规范性检查 |
